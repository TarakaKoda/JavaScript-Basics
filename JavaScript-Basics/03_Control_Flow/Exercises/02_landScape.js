// program to check if its a landscape or not
// if the width is greater than height then it is a landscape

console.log(isLandscape(800, 600))

function isLandscape(width, height) {
    return (width > height);
}