// the for of loop is used specially for the arrays objects
// this will iterate the objects in the array (similar to python for loop)

const selectedColor = ['Red', 'Green', 'Blue']

for (let color of selectedColor) {
    console.log(color)
}